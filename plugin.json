{
  "version": "0.8.0",
  "title": "Photos grid",
  "description": "Change the photos page to use a grid or masonry style.",
  "includes": ["/css/photos-grid.css"],
  "fields": [
    {
      "field": "params.all_photos",
      "label": "Include all photos in multi-photo posts, not just first photo.",
      "type": "boolean"
    }
  ]
}
